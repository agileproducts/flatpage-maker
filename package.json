{
  "name": "flatpage-maker",
  "version": "1.0.0",
  "description": "Generates an HTML page from data",
  "main": "build.js",
  "scripts": {
    "build": "node run.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "S Cornelius",
  "license": "ISC",
  "dependencies": {
    "js-yaml": "^3.12.0",
    "mustache": "^3.0.0"
  }
}
